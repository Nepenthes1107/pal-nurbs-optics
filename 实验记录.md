# 实验记录

本文件只保留当前实验可复现所需的证据、边界和负面结论。

## E20260902-PAL-FUNCTIONAL-WEIGHTED-MTF（代码就绪，未训练）

- 分支：`codex/pal-nurbs-v3-functional-weighted-mtf`，从
  `codex/pal-nurbs-v3-chain@af0d0fa` 建立。
- 目的：隔离训练目标对结果的影响；保持 109-case 九组数量、分区权重、物距、
  pupil/FFT 采样和工程约束不变，将七个真实追迹功能组目标统一为 Ahumada
  weighted-MTF loss，并统一使用固定 `0.10` 容差归一化。
- 周边左右像散组保持原来的 `surface_only` A_D 目标与 `0.80 D` 容差，不进行
  光线追迹；它们不属于本次目标切换范围。
- 身份边界：新方法使用独立 method 名和 run identity schema 11；旧目标 run、
  baseline、资格池和 checkpoint 不可 resume 或作为父源接入。
- 自动化证据：PAL 与 case-layout 定向测试 `81 passed`，完整测试 `224 passed`，
  `py_compile`、CLI `--help` 与 `git diff --check` 通过。尚未运行完整训练或正式评价，
  因此没有优化收益结论。

## E20260825-P16-R12（暂停，未完成）

- 配置：555 nm、requested pupil 1024、FFT 512、seed 42。
- identity：`9e30366cd353f39b63782aafceabacdb2b2498d8202123aff54a793b2d72c92d`。
- 本机归档：`results/archive/r12_incomplete`；其 baseline 来源文件为 `results/archive/r12_baseline_source.pt`。
- 进度：7x7 完成 10/10；11x11 完成 4/10。
- 结论：run 未完成，无最终收益结论；checkpoint 仅作为历史证据，不再作为跨平台恢复入口。
- 资源负面证据：Windows/WDDM allocator cache 会增加 host commit；逐 case 释放 graph、`empty_cache()` 和 GRIN3 activation checkpoint 用于降低资源峰值，不改变物理模型。

## E20260831-PAL-OUTER-FIELD-NONDIFF-CHECK

- 目的：核查 PAL 密集候选追迹在外围场点的失败是否也出现在独立的传统 BIOT
  不可微 PSF 链，以及 `D:\VSCODE\BIOT_vis\multi_rays.py`。
- 共同物理输入：本项目 `eye_image_glass_grad3.xlsx`，SHA-256
  `10287075eb4016675fe8da151fddf1c4b145fd2161c2afd743f183b74e759ec0`；
  Dinf、CPU、`np=32`、`ni=64`。BIOT_vis 自带同名 Excel 哈希不同，未用于对比。
- 独立实现：本项目 `multi_rays.py/optics.py` 哈希分别为
  `982a4197ba51b156f03df11c4447cfed77c63a6f54a71373ebf6a96c1e624b37`/
  `bc74e708f86d453f25e3ebfb81968bb9afe97b248066aa7b37c12c210a677cdc`；
  BIOT_vis 对应哈希为
  `04dbc365855ff0354f5501198ab621c9ba8ae7b456d60dd684a5447474707797`/
  `fbf0a5088ae6c877b9df67193fa602bda4feaabac1b82cf638d006a4d98b20bd`。
- 结果：两套不可微实现对下列场点的成功/失败判定完全一致：
  `(-45,-60)` 失败（backward surface 3，半径 52.4372 mm > 40 mm）；
  `(0,-60)` 失败（backward surface 2，40.2264 mm > 40 mm）；
  `(45,-45)` 失败（backward surface 2，40.0443 mm > 40 mm）；
  `(-12,-59)` 失败（传统链 forward surface 2，41.2635 mm > 40 mm；候选链原记录为
  NURBS denominator 非正）；`(0,-55)` 成功。`(45,45)` 的候选中心参考光线可到达
  PAL 后表面，但两套完整 pupil/PSF 链均在 forward surface 2 失败（40.3633 mm > 40 mm）。
- 结论边界：代表性失败类别与边界控制点证明外围缺失不是可微实现独有现象，主因是
  传统 BIOT 中同样存在的表面口径越界。该核查不是对 661 个失败场点逐点运行完整 PSF；
  同时必须区分“中心参考光线可达”和“完整光瞳束可用于 PSF”。BIOT_vis 的旧失败导出
  已保存在 `results/diagnostics/biot_vis_trace_check/`，不进入 Git。

## 后续正式实验边界

- 2026-08-29 合同更新：`main` 训练保持 80-case/7×7→11×11→19×19 架构，但训练前向统一改为原始 `512×512` 物理 FFT PSF；两分支固定 `legacy_pupil_phase=False`、`phase_reference="biot_reference_sphere"`、`remove_tilt=False`。训练 M2、健康检查和梯度不再使用 130×130 crop/render kernel。该改变提升 identity/schema，旧 run/checkpoint 只读、不可续跑；本次仅做代码和测试验收，不启动正式优化。

- 所有后续实验从当前源码重新开始，建立新的 run identity。
- 当前主线不再使用 Phase 编号；输出目录按 `results/optimization/run_001`、`run_002` 递增。
- 使用 Python 3.8.20、PyTorch 2.0.1、CUDA runtime 11.8。
- 仍使用真实可微追迹、GRIN3、连续 OPL、去 pupil tilt FFT PSF 和固定 80-case 合同。
- 只有 `summary.json` 存在且 `run_state.status=complete` 时，才形成正式收益结论。
- 新训练物距统一为 `D500/D1000/Dinf`，并使用修复后的孔径边界合同；旧 run 不得 resume 到该合同。
- 新训练目标中功能区保持归一化物理 PSF M2，左右周边区改用区域平均
  `A_D/Original PAL A_D`；这是新的训练 identity，旧目标 run 只能只读评价。
- 完成 run 后使用 `evaluate_pal_nurbs.py` 生成独立评价身份；评价产物不改变源 run，且旧 run 的源训练物距与新评价物距分别记录。
- 正式评价必须先完成六个条件级 HDF5 PSF 数据库并通过逐节点/逐文件哈希；
  weighted MTF 使用原始 PSF，PSF/视标拼接使用统一的 130×130 物理裁剪核。
  `blur-scale` 仅属于视标显示身份，不属于物理 PSF 或 MTF 身份。
- 不恢复历史 192-case posthoc、旧 PSF 数据库或渲染链。
