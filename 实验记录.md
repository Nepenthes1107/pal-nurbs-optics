# 实验记录

本文件只保留当前实验可复现所需的证据、边界和负面结论。

## E20260825-P16-R12（历史，暂停且未完成）

- 配置：555 nm、requested pupil 1024、FFT 512、seed 42。
- identity：`9e30366cd353f39b63782aafceabacdb2b2498d8202123aff54a793b2d72c92d`。
- 本机归档：`results/archive/r12_incomplete`；其 baseline 来源文件为 `results/archive/r12_baseline_source.pt`。
- 进度：7x7 完成 10/10；11x11 完成 4/10。
- 结论：run 未完成，无最终收益结论；checkpoint 仅作为历史证据，不再作为跨平台恢复入口。
- 资源负面证据：Windows/WDDM allocator cache 会增加 host commit；逐 case 释放 graph、`empty_cache()` 和 GRIN3 activation checkpoint 用于降低资源峰值，不改变物理模型。

## E20260826-PAL-MD-SMOKE（开发分支，非科学结论）

- 分支：`codex/pal-multi-distance-second-moment`；基线提交：`146111e chore: establish clean PAL-NURBS baseline`。
- 方法身份：固定 11×11 PAL 后表面 NURBS 参数、`D500/D2000/Dinf` 三个物距、每个物距共用同一个 11×11 FOV 网格、真实可微追迹、去 pupil tilt raw FFT PSF 和 PSF 二阶矩加权 loss。
- 固定 case 合同：`3×121=363`；`Dinf` 使用真实 `Infinity` 条件，不用有限距离替代。
- 权重输入：`inputs/pal/multidistance_weights.json`；分区总质量为 far/corridor/near 各 0.25、astig-left/right 各 0.125，分区内再按配置分配三个物距，展开权重总和为 1。
- 本次 prepare-only 输入哈希：`zones.json` = `da41e12dbf96e1f7ce2dd4127667eedba868224c83b86d32f6dd817902f6e29d`；`multidistance_weights.json` = `43f8cd46a27032576b141e6f4a5c72e23639f746f15da3bef55f3f0b8d5c9f20`。
- prepare-only smoke：使用 CPU、requested pupil 32、FFT 64，生成 363 个 case；三个物距的 `(grid_row, grid_column, field_x_deg, field_y_deg)` 完全一致；分区分类为 `stored_mask=345`、`nearest_partition_cell=18`。
- 当前代码身份下的产物：`case_payload_sha256=bd7118a9ccae729400e53b0537fc110945f5e012a7f58b81ddec7e825edc2134`，`layout_identity_sha256=0076a20b6556baa7c57b7f9bf56b458afc9412667b17c1b14ab421fa12620993`；同目录 `--resume --prepare-only` 通过布局哈希校验。
- 物理单 case smoke：raw PSF finite、非负、总能量归一化；PSF 二阶矩可反传到 11×11 NURBS 内层参数，梯度 finite 且非零。
- 最终自动化验证：`.venv\Scripts\python.exe -m pytest tests -q --basetemp .tmp_pytest_final_identity`，`131 passed`；未跳过测试。
- 边界：本节只记录布局、物理链路和梯度健康验证；没有正式训练结果，不形成收益或视觉质量结论。

## 后续正式实验边界

- 所有后续实验从当前源码重新开始，建立新的 run identity。
- 当前主线不再使用 Phase 编号；输出目录按 `results/optimization/run_001`、`run_002` 递增。
- 使用 Python 3.8.20、PyTorch 2.0.1、CUDA runtime 11.8。
- 使用真实可微追迹、GRIN3、连续 OPL、去 pupil tilt FFT PSF、固定三物距和 363-case 合同。
- 正式运行必须记录 `run_identity.json`、`case_layout.json`、权重/分区输入 SHA-256、checkpoint、history 和完整 sweep 的 health 指标。
- 只有 `summary.json` 存在且 `run_state.status=complete` 时，才形成正式收益结论。
- 不恢复历史候选点选择、80-case 目标、192-case posthoc、旧 PSF 数据库、渲染链或 refinement audit。
