# AGENTS.md

本文件约束在本项目中工作的 coding agent。使用中文回答。

## 启动检查

开始任务前：

1. 阅读 `README.md`、`CONTEXT_LOG.md`、`实验记录.md` 和本文件。
2. 查看 `git status --short --branch`，保留用户已有修改，不回滚无关内容。
3. 确认任务属于稳定 BIOT，还是当前 PAL-NURBS 80-case 优化主线。
4. 涉及训练、评估或科学结论时，先核对机器可读配置、哈希、split 和原始结果路径。

默认环境：

```powershell
cd D:\VSCODE\端到端光学设计
.\.venv\Scripts\Activate.ps1
```

Linux 云服务器上传源码后重新创建 `.venv`，使用 `source .venv/bin/activate`。不要上传或复用 Windows `.venv`。

## 工作原则

- 从第一性原理分析问题：先确认物理方程、坐标、单位、采样和数据流，再定位实现错误。
- 不做兜底实现。不得用静默回退、经验修正、自动降级或替代数据掩盖主流程错误。
- 允许缩小问题规模，但不能替换物理本质。可减少面数、视场、光线或自由度；不能使用伪 spot、伪 PSF、占位折射或无解释的平滑。
- 数值输出优先于视觉印象。必须保存并检查 finite、能量、有效光线、边缘能量、梯度、误差和单位。
- 保持结果可复现：配置、随机种子、输入哈希、代码身份、checkpoint 和选择规则必须可追溯。

## 代码边界

- `optics.py`、`multi_rays.py`、镜片几何评价和 GUI 属于稳定 BIOT；除非任务明确要求，不把实验代码塞入这些模块。
- e2e/PAL 实验代码放在 `biot/e2e`，对应测试放在 `tests/`。
- 保持 `multi_rays.py` 位置参数接口兼容。新增 CLI 参数要有默认值，不能破坏 GUI、service 或批处理调用。
- 不覆盖主 Excel、Z1、原始 GridSag、参考数据或历史正式 run。

## 物理与数值约束

- 长度、角度、波长、像素和空间频率必须显式标注单位；禁止未说明的单位混用。
- 物理 PSF 必须 finite、非负并按总能量归一化。显示增强 PSF 不得用于 MTF、loss 或物理指标。
- MTF 必须从能量归一化 PSF 计算并做 DC 归一化。
- 可微核心不得用 `.detach()`、`.cpu()`、`.numpy()`、`.item()`、NumPy、SciPy 或 OpenCV 参与需要反传的图。
- 新 tensor 应继承参考 tensor 的 dtype/device；核心 shape、坐标方向、单位和 autograd 状态要在接口或 metadata 中说明。
- 无效光线、全反射、零能量和非有限值必须显式记录或失败，不能静默丢弃。
- GridSag raw/physical Y 约定必须保持一致；导出和显示方向不得凭图像观感修改。
- 不通过滤波、插值、裁剪、形态学操作或后处理强行满足光学、面形或区域门槛。

## PAL 与训练约束

- PAL 更新必须使用真实可微几何追迹和去 pupil tilt FFT PSF；离线 `PSF_D*.mat` 不得进入 PAL 反向传播。
- `best_feasible` 必须基于完整覆盖周期，且周期内所有工程和健康约束通过。不得用 `best_image`、`latest` 或局部低 loss 兜底。
- validation 用于选择；test 只能在 final selection 后访问。数据隔离以逐文件 SHA-256 为准，文件名或目录名不构成证据。
- 改变约束、阈值、数据、seed、预算或选择规则时建立新实验身份，不原位改写历史结论。
- 当前去 tilt、单波长结果不能外推为色差、棱镜、真实视物位置或几何畸变合格。

## 长任务与恢复

- 长时间训练和评估必须可恢复，持续写 checkpoint、状态、日志和退出信息。
- 中断后先检查进程、checkpoint、状态文件和结果完整性，再从最近可验证状态恢复；不要无故重跑已完成阶段。
- 不因额度、显存、启动器或超时问题缩小正式目标、降低采样或放宽门槛。
- 监控长训练时按项目约定低频检查，通常每 30 分钟一次；任务结束后核验退出码和产物完整性。

## 测试与验收

- 物理修改至少检查 finite、非负、能量归一化、有效光线、单位、shape 和梯度。
- 只看 PNG 不算验收；与 BIOT/Zemax 对比要使用一致归一化和数值指标。
- 测试失败应修复原因。不得通过跳过测试、放宽容差或添加回退路径制造通过。

常用最小验证见 `README.md`。执行过的命令、通过数和已知 warning 记录到 `CONTEXT_LOG.md`；形成科学结论时另写 `实验记录.md`。

## Git 与文件

- 不回滚用户未提交修改，不清理无关未跟踪文件。
- 不使用破坏性 Git 命令，除非用户明确要求并确认范围。
- 生成结果默认写入 `results/`，不要提交数据集、checkpoint、缓存、Office 临时文件或大型导出。
- 修改前读清上下文，保持改动范围聚焦；不要顺手重构无关模块。
- 提交前检查 `git status --short --branch` 和实际 diff，确认没有带入用户数据或无关文件。

## 文档职责

根目录只维护四份 Markdown：

- `README.md`：当前项目说明、主线、命令和验证入口。
- `CONTEXT_LOG.md`：当前工程状态、有效契约、限制和待办；不追加逐会话流水账。
- `实验记录.md`：有证据价值的实验、负面结果和结论边界。
- `AGENTS.md`：协作与质量约束；不堆叠具体实现细节或历史数值。

新增/废弃 CLI 或改变默认行为时更新 README 和 CONTEXT_LOG。新增训练、正式评估、消融、基线、失败证据或科学结论时更新实验记录。重复 smoke、纯重构和无新结论的例行测试不写入实验记录。
